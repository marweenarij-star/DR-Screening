# AI Service runtime requirements
# Only what ai-service/main.py and training modules actually import at runtime.
# scipy and scikit-learn are NOT needed here (only used inside training functions,
# never at module import time or during inference).

# --- Web framework ---
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
starlette>=0.27.0

# --- Environment ---
python-dotenv==1.0.0

# --- Image processing ---
Pillow>=10.1.0
opencv-python-headless>=4.8.1
pydicom>=2.4.4

# --- ML / inference ---
numpy>=1.26.0
torch>=2.0.0
torchvision>=0.15.0
# Pinned to match the version used to train the EfficientNetB3 checkpoint.
# A different timm version renames internal efficientnet_b3 layers and breaks load_state_dict.
timm==1.0.24

# --- HuggingFace (ViT model download) ---
transformers>=4.35.0
huggingface_hub>=0.19.0
safetensors>=0.4.0
requests>=2.31.0

# --- Training module imports (loaded at import time by train_resnet50 / train_efficientnet) ---
pandas>=2.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
